/// @desc 

// Apply a static image to the UI element
image_index = 0;
image_speed = 0;

sprite_index = rps_get_sprite(buttonType);

// Vars for making fancy buttons!
uiGrow = [1, 0.1, 2];		// [current_value, speed, max_value]